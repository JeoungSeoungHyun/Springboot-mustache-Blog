{{>/layout/header}}
<!-- 컨테이너 시작 -->
<div class="container mt-3">

    <!-- 게시글 아이템 시작 -->
    {{#posts}}
    <div class="card mb-3">
        <div class="card-body d-flex justify-content-between  align-items-center">
            <h5 class="card-title">{{title}}</h5>
            <a href="/post/{{id}}" class="btn btn-cs text-light">상세보기</a>
        </div>
    </div>
    <!-- 게시글 아이템 끝 -->
    {{/posts}}
</div>
<!-- 글 목록 끝 -->
{{>/layout/footer}}